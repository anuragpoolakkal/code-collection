// C program to print pell series.

//CONTRIBUTE TO THE CODE

/*

#include <stdlib.h>

int pell(int num)
{
    if (num <= 2)
        return num;
    else
        return (2 * pell(num - 1) + pell(num - 2));
}

void main()
{
    int num;
    printf("Enter number/limit: ");
    scanf("&d", num);
    printf("Pell number at position " << num << " is: " << pell(num);
    printf("\nPell series till position " << num << " is: ";
    for (int i = 0; i <= num; i++)
    {
        printf(pell(i) << "  ";
    }
}

*/